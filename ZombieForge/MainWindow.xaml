<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ZombieForge.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZombieForge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <NavigationView x:Name="NavView"
                    SelectionChanged="NavView_SelectionChanged"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="True">
        <NavigationView.MenuItems>
            <NavigationViewItem x:Uid="NavHome" Tag="home">
                <NavigationViewItem.Icon><FontIcon Glyph="&#xE80F;"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="NavConfig" Tag="config">
                <NavigationViewItem.Icon><FontIcon Glyph="&#xE9E9;"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="NavHistory" Tag="history">
                <NavigationViewItem.Icon><FontIcon Glyph="&#xE81C;"/></NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <NavigationView.PaneFooter>
            <StackPanel Spacing="8" Margin="16 0 16 16"
                        DataContext="{x:Bind ViewModel, Mode=OneWay}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Ellipse Width="8" Height="8" VerticalAlignment="Center"
                             Fill="{Binding IsGameRunning, Converter={StaticResource BoolToColor}}"/>
                    <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <ComboBox ItemsSource="{x:Bind ViewModel.AvailableGames}"
                          SelectedIndex="{x:Bind ViewModel.SelectedGameIndex, Mode=TwoWay}"
                          HorizontalAlignment="Stretch"/>
            </StackPanel>
        </NavigationView.PaneFooter>

        <Frame x:Name="ContentFrame"/>
    </NavigationView>
</Window>
